version: "3.1"

rules:
  - rule: greet user
    steps:
      - intent: greet
      - action: utter_greet

  - rule: handle fallback
    steps:
      - intent: nlu_fallback
      - action: utter_fallback
